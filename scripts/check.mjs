import fs from'node:fs';import path from'node:path';const root=path.resolve(import.meta.dirname,'..');let n=0,e=[];function w(d){for(const x of fs.readdirSync(d,{withFileTypes:true})){if(['.git','node_modules'].includes(x.name))continue;const p=path.join(d,x.name);if(x.isDirectory())w(p);else if(x.name.endsWith('.html')){n++;const s=fs.readFileSync(p,'utf8');if(!s.includes('assets/styles.css'))e.push(`${p}: CSS ontbreekt`);if(!s.includes('noindex,follow'))e.push(`${p}: robots ontbreekt`)}}}w(root);if(e.length){console.error(e.join('\n'));process.exit(1)}console.log(`${n} pagina's gecontroleerd.`);
